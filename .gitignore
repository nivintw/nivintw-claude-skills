### Direnv ###
.direnv/

### Dotenv / secrets ###
.env

### macOS ###
.DS_Store
.AppleDouble
.LSOverride
._*

### Editors ###
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*~

### castify (generated cast output from the example scripts) ###
casts/
*.txt
# ...but never the REUSE license texts — they're .txt and MUST be tracked.
!LICENSES/*.txt
# ...nor the template-reconcile manifests (divergence registry + template-owned list) that
# the synced-files test and scripts/check_managed_files.sh read — they're .txt and MUST track.
!tests/template-divergences.txt
!tests/template-owned-files.txt

### Python (dev-kit's generate-docs build.py runs under uv) ###
__pycache__/
*.py[cod]

### dev-kit:ship continuity files (per-run progress; never committed) ###
.ship/

### superpowers:brainstorming local artifacts (mockups/companions, never committed) ###
.superpowers/

### MkDocs ###
site/
# The social-cards plugin caches generated og:image renders here across builds.
.cache/
